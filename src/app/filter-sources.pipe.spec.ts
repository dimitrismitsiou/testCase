import { FilterSourcesPipe } from './filter-sources.pipe';

describe('FilterSourcesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSourcesPipe();
    expect(pipe).toBeTruthy();
  });
});
